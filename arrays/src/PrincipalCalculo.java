public class PrincipalCalculo {
    public static void main(String[] args) {

        MediaCalculo mediaCalculo = new MediaCalculo();

        double resultado = mediaCalculo.calculo(30, 20, 10, 40);
        System.out.println(resultado);
    }
}
